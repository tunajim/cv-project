import React, { Component } from "react";
import "../styles/header.css";

class Header extends Component {
    render() {
        return (
            <header className="App-header">
                <h1 id="logo">CV Application Maker</h1>
            </header>
        )
    }
}

export default Header;